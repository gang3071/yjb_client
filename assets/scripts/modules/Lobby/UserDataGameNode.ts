import * as cc from 'cc';
import { BaseView } from '../../base/frame/BaseView';
const { ccclass, property } = cc._decorator;

@ccclass('UserDataGameNode')
export class UserDataGameNode extends BaseView {
    start() {

    }

    update(deltaTime: number) {
        
    }
}


